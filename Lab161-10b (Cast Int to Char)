// Lab161-10b
// Cast Int to Char
// Write a java program that:
// Prints to the screen the character equivalence of an inputted decimal value. 
// The format of the output can be seen below. 
// Stops printing when the decimal value for 'ESC' is inputted.

import java.util.Scanner;
public class Casting2 {
public static void main(String[]args) {
	Scanner input = new Scanner(System.in);
	boolean T = true;
	String sen = "";

	while(T == true) {
	    
	    int num = input.nextInt();
		if(num == 27) {
		T = false;
		break;
	    }
		
	sen	+= " "+(char)num;
	
	}
  
		System.out.println(sen);

    }
}
